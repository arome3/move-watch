# MoveWatch API Environment Variables

# Server Configuration
PORT=4000
NODE_ENV=development

# Frontend URL (for CORS and share links)
FRONTEND_URL=http://localhost:3000

# Movement Network (mainnet, testnet, devnet)
MOVEMENT_NETWORK=testnet

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/movewatch

# Redis (for caching and cooldowns)
REDIS_URL=redis://localhost:6379

# Authentication (must match frontend NextAuth secret)
NEXTAUTH_SECRET=your-nextauth-secret-here

# ============================================================================
# x402 Payment Configuration
# ============================================================================

# Set to "true" to disable all payment requirements (for development)
DISABLE_PAYMENTS=true

# MoveWatch treasury wallet address on Movement Network (receives payments)
MOVEWATCH_PAYMENT_ADDRESS=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# Movement Network RPC endpoint for payment settlement
MOVEMENT_TESTNET_RPC=https://aptos.testnet.bardock.movementlabs.xyz/v1

# Payment network (movement-testnet or movement-mainnet)
PAYMENT_NETWORK=movement-testnet

# ============================================================================
# Email Configuration (Resend)
# ============================================================================

# Resend API Key for sending email notifications
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Default "from" email address
EMAIL_FROM=MoveWatch <noreply@movewatch.io>

# ============================================================================
# Secrets Encryption
# ============================================================================

# 32-byte hex key for encrypting secrets (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SECRETS_ENCRYPTION_KEY=your-64-character-hex-key-here

# ============================================================================
# AI Analysis (Anthropic Claude)
# ============================================================================

# API key for AI-powered security analysis (multi-stage LLM analysis)
# Get yours at: https://console.anthropic.com/
# Guardian works without this key, but AI features (extended thinking, agentic analysis) will be skipped
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
