# MoveWatch Web Environment Variables

# =============================================================================
# Required Configuration
# =============================================================================

# NextAuth.js secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-nextauth-secret-here

# Application URL
NEXTAUTH_URL=http://localhost:3000

# API URL
NEXT_PUBLIC_API_URL=http://localhost:4000

# Database URL (PostgreSQL) - Required for Prisma adapter
DATABASE_URL=postgresql://user:password@localhost:5432/movewatch

# =============================================================================
# Authentication Providers (configure at least one)
# =============================================================================

# GitHub OAuth
# Create app at: https://github.com/settings/developers
# Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_ID=your-github-client-id
GITHUB_SECRET=your-github-client-secret

# Google OAuth
# Create app at: https://console.cloud.google.com/apis/credentials
# Callback URL: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# Email Configuration (for magic link auth)
# =============================================================================

# Resend API Key (get at https://resend.com)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Default sender email (must be verified in Resend)
EMAIL_FROM=MoveWatch <noreply@movewatch.io>
